<h1>Modification canal de distribution</h1>

<%= form_for @channel, :url=> {:action=>:update, :id=>@channel.id} do |f| %>
  <% if @channel.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize @channel.errors.count, "erreur", "erreurs" %> :</h2>
          <% @channel.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </div>
  <% end %>

  <p>
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </p>
  <p>
    <%= f.label "Nom" %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Enregistrer" %>
  </p>
<% end %>

<%= link_to 'Afficher', :action=>:show, :id=>@channel.id  %> |
<%= link_to 'Retour', admin_channels_path %>
